-- 신청을 오늘 오후 5시에 연다.
--
-- 21시로 잡아 두었는데 그 전에 열기로 했다. 마감(멘토 시간이 끝날 때)과
-- 시작 시각은 그대로다.
--
-- 시간대를 반드시 붙인다. 안 붙이면 서버 시간대로 읽히는데 배포 서버는
-- UTC라 아홉 시간 밀린다 — 0033에서 그렇게 한 번 어긋났다.
update public.mentor_sessions
set opens_at = timestamptz '2026-09-06 17:00:00+09';
